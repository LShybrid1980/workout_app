<tr>
  <td><%= upper_back.name %></td>
  <td><%= upper_back.weight %></td>
  <td><%= upper_back.set %></td>
  <td><%= upper_back.rep %></td>
  <td><%= link_to 'Show', upper_back %></td>
  <td><%= link_to 'Edit', edit_upper_back_path(upper_back) %></td>
  <td><%= link_to 'Destroy', upper_back, method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>