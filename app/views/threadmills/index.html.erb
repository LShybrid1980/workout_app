<div class='page-header'>
  <h1>Your Current Threadmill Workout Data</h1>
</div>

<table class='table table-striped table-hover'>
  <thead>
    <tr>
      <th>Name</th>
      <th>Speed</th>
      <th>Time</th>
      <th>Incline</th>   
      <th>Distance</th> 
    </tr>
  </thead>

  <tbody>
    <% @threadmills.each do |threadmill| %>
      <tr>
        <td><%= link_to threadmill.name, threadmill %></td>
        <td><%= threadmill.names %></td>
        <td><%= link_to threadmill.speed, threadmill %></td>
        <td><%= threadmill.speeds %></td>
        <td><%= link_to threadmill.time, threadmill %></td>
        <td><%= threadmill.times %></td>
        <td><%= link_to threadmill.incline, threadmill %></td>
        <td><%= threadmill.inclines %></td>
        <td><%= link_to threadmill.distance, threadmill %></td>
        <td><%= threadmill.distances %></td>
        <td><%= link_to 'Edit', edit_threadmill_path(threadmill) %> | 
            <%= link_to 'Destroy', threadmill, method: :delete, data: { confirm: 'Are you sure?' } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Threadmills Workout Data', new_threadmill_path, class: 'btn btn-primary' %>
