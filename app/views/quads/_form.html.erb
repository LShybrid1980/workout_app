<div class='well bs-component col-lg-8'>
  <%= form_for(@quad, url: workout_data_quads_path(@workout_data, @quad)) do |f| %>
    <% if @quad.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@quad.errors.count, "error") %> prohibited this quads workout data from being saved:</h2>

        <ul>
        <% @quad.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class='form-group'>
        <%= f.label "Name", class: 'col-lg-2' %>
        <div class='col-lg-10'>
          <%= f.select(:status_type, options_for_select([['Barbell Full Squat', 'Barbell Full Squat (Quads)'], ['Barbell Walking Lunge', 'Barbell Walking Lunge (Quads)'], ['Barbell Step Ups', 'Barbell Step Ups (Quads)'], ['Barbell Lunge', 'Barbell Lunge (Quads)'], ['Bench Jump', 'Bench Jump (Quads)'], ['Bodyweight Lunge', 'Bodyweight Lunge (Quads)']])) %><br><br>
        </div>  
      </div>

      <div class='form-group'>
        <%= f.label :weight, class: 'col-lg-2' %>
        <div class='col-lg-10'>
          <%= f.number_field :weight, class: 'form-control', autofocus: true %><br>
        </div>
      </div>

      <div class='form-group'>
        <%= f.label :set, class: 'col-lg-2' %>
        <div class='col-lg-10'>
          <%= f.number_field :set, class: 'form-control', autofocus: true %><br>
        </div>
      </div>

      <div class='form-group'>
        <%= f.label :rep, class: 'col-lg-2' %>
        <div class='col-lg-10'>
          <%= f.number_field :rep, class: 'form-control', autofocus: true %><br>
        </div>  
      </div>

    <div class="actions">
      <%= f.submit %>
    </div>
  <% end %>
</div>  
