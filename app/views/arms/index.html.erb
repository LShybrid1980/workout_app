<div class='page-header'>
  <h1>Your Current Arms Workout</h1>
</div>

<table class='table table-striped table-hover'>
  <thead>
    <tr>
      
          
    </tr>
  </thead>

  <tbody>
    <% @arms.each do |arm| %>
      <tr>
        <td><%= link_to 'Edit', edit_arm_path(arm) %> | 
            <%= link_to 'Destroy', arm, method: :delete, data: { confirm: 'Are you sure?' } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Arms Workout Data', new_arm_path, class: 'btn btn-primary' %>