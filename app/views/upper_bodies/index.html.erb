<div class='page-header'>
  <h1>Your Current Upper Body Workout</h1>
</div>

<table class='table table-striped table-hover'>
  <thead>
    <tr>
      
          
    </tr>
  </thead>

  <tbody>
    <% @upper_bodies.each do |upper_body| %>
      <tr>
        <td><%= link_to 'Edit', edit_upper_body_path(upper_body) %> | 
            <%= link_to 'Destroy', upper_body, method: :delete, data: { confirm: 'Are you sure?' } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Upper Body Workout Data', new_upper_body_path, class: 'btn btn-primary' %>
        