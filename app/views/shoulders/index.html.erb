<div class='page-header'>
  <h1>Your Current Shoulder Workout</h1>
</div>

<table class='table table-striped table-hover'>
  <thead>
    <tr>
      <th>Name</th>
      <th>Weights</th>
      <th>Sets</th>
      <th>Reps</th>    
    </tr>
  </thead>

  <tbody>
    <% @shoulders.each do |shoulder| %>
      <tr>
        <td><%= link_to shoulder.status_type, shoulder %></td>
        <td><%= link_to shoulder.weight, shoulder %></td>
        <td><%= link_to shoulder.set, shoulder %></td>
        <td><%= link_to shoulder.rep, shoulder %></td>
        <td><%= link_to 'Edit', edit_workout_data_shoulder_path(shoulder) %> | 
            <%= link_to 'Destroy', shoulder, method: :delete, data: { confirm: 'Are you sure?' } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Shoulders Workout Data', new_workout_data_shoulder_path, class: 'btn btn-primary' %>
        
        
        
        