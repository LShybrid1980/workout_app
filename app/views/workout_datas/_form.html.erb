<%= form_for(workout_data) do |f| %>
  <% if workout_data.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(workout_data.errors.count, "error") %> prohibited this workout data from being saved:</h2>

      <ul>
      <% workout_data.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="col-md-6 col-sm-12" class="form-group">
    Choose a date:
    <label for="packt">Please Select date</label>
    <input id="packt" class="form-control" type="date" value="2017-01-01"><br><br>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
