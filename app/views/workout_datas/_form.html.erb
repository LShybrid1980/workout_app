<%= form_for(@workout_data, url: workout_datas_path(@workout_data)) do |f| %>
  <% if @workout_data.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@workout_data.errors.count, "error") %> prohibited this workout data from being saved:</h2>

      <ul>
      <% @workout_data.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class='form-group'>
        <%= f.label :date, class: 'col-lg-2' %>
        <div class='col-lg-10'>
          <%= f.date_field :date, class: 'form-control', autofocus: true %><br>
        </div>
      </div>

  <div class="actions">
      <%= f.submit %>
  </div>
  
<% end %>
