<%= form_for(@cardio, url: cardios_path(@cardio)) do |f| %>
  <% if @cardio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cardio.errors.count, "error") %> prohibited this workout data from being saved:</h2>

      <ul>
      <% @cardio.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <!-- <div class="col-md-6 col-sm-12" class="form-group">
    Choose a date:
    <label for="packt">Please Select date</label>
    <input id="packt" class="form-control" type="date" value="2017-01-01"><br><br>
  </div> -->

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>